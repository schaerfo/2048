#ifndef GLOBALS_H
#define GLOBALS_H

typedef unsigned int grid_t;
typedef unsigned int grid_size_t;

typedef enum {
    MOVE_UP, MOVE_DOWN, MOVE_LEFT, MOVE_RIGHT,
    HELP, INFO, QUIT, INVALID
} user_action;

#endif
